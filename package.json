{
  "name": "mon-site",
  "version": "1.0.0",
  "description": "Description de mon site",
  "scripts": {
    "build": "node generate-posts-json.js && echo 'Build completed!'",
    
    "deploy": "netlify deploy"
  },
  "dependencies": {
    "marked": "^14.1.2"
  }
}
